\documentclass{minimal}
\usepackage[pdftex,active,tightpage]{preview}
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage{mathpazo}
\usepackage{tikz}
\usetikzlibrary{calc,shapes,arrows}

\begin{document}
\input{include/flujo.tex}
\begin{preview}
\begin{tikzpicture}[node distance=12ex, auto]
    \node (start) { };
    \node [decision, below of=start] (cond) { condition };
    \node [stmt, right of=cond, node distance=8em] (iter) { iteration };
    \node [below of=cond] (end) { };

    \path [conn] (start) -- (cond);
    \path [conn] (cond) --
                 node[very near start] { yes }
                 (iter);
    \path [conn] (iter.north) |-
                 ($ (start)!.5!(cond.north) $);
    \path [conn] (cond) --
                 node[very near start] { no }
                 (end);
\end{tikzpicture}
\end{preview}
\end{document}
